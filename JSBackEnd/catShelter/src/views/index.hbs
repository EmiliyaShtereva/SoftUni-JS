<main>
    <section class="cats">
        <ul>
            {{#each cats}}
                {{>catTemplate}}
            {{else}}
                <h3>There are no cubes!</h3>
            {{/each}}
        </ul>
    </section>
</main>

